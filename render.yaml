services:
  - type: web
    name: awesometodos-backend
    runtime: node
    buildCommand: "npm install --prefix server"
    startCommand: "node server/index.js"
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 5000
