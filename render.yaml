services:
  - type: web
    name: awesometodos-backend
    runtime: node
    buildCommand: "npm run build"
    startCommand: "npm start"
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 5000
